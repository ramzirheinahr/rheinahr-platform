// Private Supabase Storage bucket holding worker profile photos and uploaded
// documents. Files are only ever served through short-lived signed URLs.
export const WORKER_FILES_BUCKET = "worker-files";
